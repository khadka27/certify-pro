# Application URL (required for production)
# This is used for generating absolute URLs in PDFs, emails, and exports
NEXT_PUBLIC_APP_URL=https://certify.fakereviewgenerator.com

# Port for Next.js standalone server (must match NGINX proxy_pass)
PORT=3044

# Node environment
NODE_ENV=production
